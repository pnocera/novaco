log_level = "{{.LogLevel}}"

hostname = "{{.Hostname}}"

port = {{.Port}}

key_dir = "{{.KeyDir}}"

dir = "{{.Dir}}"

git_path = "{{.GitPath}}"

git_user = "{{.GitUser}}"

auto_create = {{.AutoCreate}}

auto_hooks = {{.AutoHooks}}

hook_scripts = {{.HookScripts}}

auth = {{.Auth}}